export type TUserData = {
  name: string;
  avatarUrl: string;
  email: string;
  token: string;
};


export type TUserRegistrationData = {
    name: string;
    email: string;
    password: string;
  }
